import graph = require("@gravity-ui/graph/playwright");

const GraphPOConstructor: typeof graph.GraphPO = graph.GraphPO;
const cameraState: graph.GraphCameraState = { x: 0, y: 0, scale: 1 };

void GraphPOConstructor;
void cameraState;
